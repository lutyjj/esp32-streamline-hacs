{
  "config": {
    "abort": {
      "already_configured": "This device is already configured.",
      "reauth_successful": "The admin key was updated.",
      "reconfigure_successful": "The device settings were updated."
    },
    "error": {
      "cannot_connect": "Could not connect to the device. Check the URL and the network.",
      "invalid_auth": "The device rejected this admin key.",
      "invalid_response": "The device returned an unexpected response. Update the device and this integration to matching versions.",
      "invalid_url": "Enter an HTTP or HTTPS device root URL without a path or query."
    },
    "step": {
      "reauth_confirm": {
        "data": {
          "admin_key": "Admin key"
        },
        "description": "The device rejected the saved admin key. Enter the key configured on the device.",
        "title": "Update the admin key"
      },
      "reconfigure": {
        "data": {
          "admin_key": "Admin key",
          "device_url": "Device URL"
        },
        "data_description": {
          "admin_key": "Admin key configured on the device. Leave empty for read-only access.",
          "device_url": "Root URL of the ESP32 device API, for example http://192.0.2.1."
        },
        "title": "Reconfigure the StreamLine device"
      },
      "user": {
        "data": {
          "admin_key": "Admin key",
          "device_url": "Device URL"
        },
        "data_description": {
          "admin_key": "Admin key configured on the device. Leave empty for read-only access.",
          "device_url": "Root URL of the ESP32 device API, for example http://192.0.2.1."
        },
        "title": "Connect to a StreamLine device"
      }
    }
  },
  "entity": {
    "binary_sensor": {
      "playing": {
        "name": "Playing"
      }
    },
    "number": {
      "adc_attenuation": {
        "name": "ADC attenuation"
      },
      "input_gain": {
        "name": "Input gain"
      }
    },
    "select": {
      "input": {
        "name": "Input"
      }
    },
    "sensor": {
      "health": {
        "name": "Health",
        "state": {
          "blocking": "Blocking",
          "info": "Info",
          "ok": "OK"
        }
      },
      "network_errors": {
        "name": "Network errors"
      },
      "peak_level": {
        "name": "Peak level"
      },
      "wifi_signal": {
        "name": "Wi-Fi signal"
      }
    },
    "switch": {
      "analog_passthrough": {
        "name": "Analog passthrough"
      }
    }
  }
}
